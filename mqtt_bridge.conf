
[mqtt]
broker = localhost
port = 1883
keepalive = 60
username =
password =
topic_from_mesh = from_meshtastic
topic_from_aredn = from_aredn
topic_alerts = alerts/emergency

[meshtastic]
device =  # Leave empty for auto-detect (e.g., /dev/ttyUSB0)
reconnect_attempts = 5
reconnect_delay = 10

[ai_service]
url = http://localhost:8000/completion
timeout = 5
enabled = True

[general]
queue_size = 100
retry_interval = 30
